extends ../base
include ../mixins/video.pug

block content
    div.profile__data
        div
            img(src=user.avatarUrl).big-avatar      
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message No videos found