mixin video(video)
    div     
        h4
        a(href=`/videos/${video.id}`)=video.title
        ul 
            li #{video.rating} / 5
            li #{video.views} #{video.views > 1 ? "views" : "view"}
            li uploaded : #{video.upload}
            li #{video.comments} comments
            
